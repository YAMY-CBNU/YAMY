pandas
scikit-learn
datasets==3.1.0
transformers==4.46.3
tokenizers==0.20.3
accelerate==1.1.1
peft==0.13.2
trl==0.12.2
fsspec==2024.9.0

# CUDA 12.1 GPU build for NVIDIA GPUs. If you need CPU-only or another CUDA
# version, install torch from https://pytorch.org/get-started/locally/ first.
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
tqdm

# Optional for QLoRA 4-bit loading.
# On Windows this can be difficult to install, so keep USE_4BIT=False unless
# your environment supports bitsandbytes.
# bitsandbytes
